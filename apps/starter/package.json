{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:client && npm run build:server && npm run copy:manifest",
    "build:client": "vite build",
    "build:server": "node scripts/build-server.js",
    "copy:manifest": "cp appsscript.json dist/",
    "deploy": "npm run build && cd dist && clasp push",
    "preview": "vite preview"
  },
  "dependencies": {
    "@gas-framework/core": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "esbuild": "^0.24.0",
    "esbuild-gas-plugin": "^0.8.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
